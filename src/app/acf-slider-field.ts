import { MediaSize } from './media-size';

export class AcfSliderField {
    featureIndustriasImage: this;
    title: string;
    url: string;
    alt: string;
    width: number;
    height: number;
    sizes: MediaSize;
}
